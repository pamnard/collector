/**
 * Public entry for dashboard list state. Implementation lives under
 * `./dashboard/` — keep this path stable for AppLayout / grid / table.
 */
export {
  DASHBOARD_PREFETCH_SIZE,
  DEFAULT_DASHBOARD_SORT,
  useDashboardItems,
  type UseDashboardItemsResult,
} from "./dashboard/useDashboardItems";
