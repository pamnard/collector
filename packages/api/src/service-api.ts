/**
 * Domain port contracts for CollectorService (#361 / #922).
 * Implementation modules live under `./service-api/`; this file keeps the
 * historical import path `./service-api.js` stable for in-package consumers.
 */
export * from "./service-api/index.js";
